import { FC } from 'react';
import Directory from './Directory';
const HomePage: FC = () => {
  return (
    <div className="relative">
      <Directory />
    </div>
  );
};

export default HomePage;
